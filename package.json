{
  "name": "mail-archiver",
  "version": "1.2.1",
  "private": true,
  "description": "Mail Archiver - The Mail Backup Solution",
  "license": "AGPL-3.0-or-later",
  "author": "sphings79",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "workspaces": [
    "packages/core",
    "packages/server",
    "packages/web",
    "packages/desktop"
  ],
  "scripts": {
    "build": "npm run build --workspace @mail-archiver/core && npm run build --workspace @mail-archiver/server && npm run build --workspace @mail-archiver/web",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "dev": "npm run dev --workspace @mail-archiver/desktop",
    "dev:server": "npm run dev --workspace @mail-archiver/server",
    "dev:web": "npm run dev --workspace @mail-archiver/web",
    "dist:mac": "npm run build && npm run dist --workspace @mail-archiver/desktop"
  },
  "devDependencies": {
    "typescript": "^7.0.2",
    "vitest": "^4.1.11"
  }
}
